"""
auth is the Blueprint responsible for user registration and authentication.
Templates are in folder `templates` in the main app folder `localbin`
Static files are also in `localbin` under `static/styles` and `static/js`
"""